import styled from 'styled-components/native';

import { Theme } from '@styles/theme';

export const PaymentWayStyles = {
  Text: styled(Theme.Text)`
    font-size: 8.5px;
  `,
  ButtonWrapper: styled.View`
    text-color: black
  `,
};
